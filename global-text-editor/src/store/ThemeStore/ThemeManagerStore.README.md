
# ThemeManagerStore
Since the app grows and supports theme color change, it has become unmanagable to keep track of colors across the app. Instead of setting separate colors all around in many elements, this intends to provide consistent and managable color styles.
Therefore, this provides colors to some of major components with values mainly defined in `env`.

