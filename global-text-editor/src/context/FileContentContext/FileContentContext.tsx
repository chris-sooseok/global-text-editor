import React, { createContext } from 'react'

const FileContentContext = createContext<any>({})

function FileContentProvider({ children }: any) {
    
}

export default FileContentProvider
export { FileContentContext }