

# Context re-rendering behavior
Even if a component doesn't use the context value, if that component is wrapped by the context provider, the component will be re-rendered unless you use memo to prevent this

